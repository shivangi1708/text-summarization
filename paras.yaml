TrainingArguments:
  num_train_epochs: 2
  warmup_steps: 0
  per_device_train_batch_size: 16
  per_device_eval_batch_size: 16
  weight_decay: 0.01
  logging_steps: 50
  evaluation_strategy: steps
  eval_steps: 200
  save_steps: 500 # integer, not scientific notation
  save_total_limit: 2
  gradient_accumulation_steps: 4
  logging_dir: logs

